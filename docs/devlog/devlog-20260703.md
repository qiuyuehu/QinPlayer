# QinPlayer 开发日志 - 2026-07-03

## 播放队列面板优化 + queuePanel开关

### 背景
播放队列面板已实现基础功能，需要优化 UI 和新增独立开关。

### 改动内容

#### 1. 砍掉历史记录标签页
- 删除 activeTab 状态和标签页切换逻辑
- 只保留当前队列渲染
- header 简化：标题"播放队列" + 歌曲数 + 关闭按钮

#### 2. 清空队列逻辑修正
- 保留当前歌曲 + 之前的，只清空之后的
- 按钮文案："清空后续队列"
- 逻辑：`playlist.slice(0, currentIndex + 1)`

#### 3. 返回动画串行
- 歌词退出完再显示主页面
- 使用 showMainContent 控制
- timerRef + rafRef 双重清理，防止快速切换残留
- 依赖：`[activeNav, featureFlags.lyrics, lyricsVisible]`

#### 4. 固定宽度 320px
- `width: min(320px, 100vw)`

#### 5. 封面缩略图 36x36px
- 自定义歌曲列表渲染，不复用 SongList
- 有 coverPath 且未失败：用 `window.electronAPI.getCoverUrl()` 显示图片
- 无封面或加载失败：CSS 占位（IconMusic 图标）
- brokenCoverIds 状态管理封面加载失败

#### 6. 自动滚动
- 使用 itemRefs + scrollIntoView({ block: 'nearest', behavior: 'smooth' })

#### 7. 播放逻辑
- 参考 SongList.tsx 的 handlePlay
- setCurrentTrack + setPlaying(true)
- songs:recordPlay（受 featureFlags.recent 控制）
- songs:updatePlayCount

#### 8. queuePanel feature flag
- FeatureFlagKey 类型新增 `'queuePanel'`
- FeatureFlags interface 新增 `queuePanel: boolean`
- FEATURE_FLAG_KEYS 和 DEFAULT_FEATURE_FLAGS 新增 `queuePanel: true`
- PlayerBar 按钮和面板渲染加 `featureFlags.queuePanel` 守卫

### 改动文件
- `src/components/PlaylistPanel.tsx` — 重构
- `src/styles/playlist-panel.css` — 重构
- `src/components/Content.tsx` — 增强
- `src/types/ipc.ts` — 新增 queuePanel
- `src/utils/featureFlags.ts` — 新增 queuePanel
- `src/components/PlayerBar.tsx` — 增强

### 验证结果
- npx tsc --noEmit：通过
- npm test：11 文件 / 132 用例全绿
- npm run build：通过

### 变更汇总
| 文件 | 改动 |
|------|------|
| `src/components/PlaylistPanel.tsx` | 砍掉标签页、封面缩略图、省略号、清空逻辑、播放逻辑 |
| `src/styles/playlist-panel.css` | 固定宽度、封面样式、省略号样式 |
| `src/components/Content.tsx` | 返回动画串行 + timer/raf 清理 |
| `src/types/ipc.ts` | FeatureFlagKey + FeatureFlags 新增 queuePanel |
| `src/utils/featureFlags.ts` | 新增 queuePanel flag |
| `src/components/PlayerBar.tsx` | 按钮和面板加 queuePanel 守卫 |
