# Handoff — 2026-07-10

## 项目当前状态

- 项目：QinPlayer（纯本地音乐播放器）
- 技术栈：Electron + React 18 + TypeScript + Zustand + electron-vite
- 路径：`C:\Users\秋月\Desktop\QinPlayer\`
- 测试：15 个文件、169 个测试全绿
- 最近完成：歌词界面三项修复（切歌闪烁、全屏空白、动画优化）、SPEC.md 架构总览

## 待做需求（按优先级）

### 已完成（标记 y）

1. ✅ 窗口尺寸持久化（`electron/windowBounds.ts` + `windowSizePersist` feature flag）
3. ✅ 歌词优化（trackTransitionRef、layoutRevision、方向动画）

### 待做

### 2. 听歌时长记录（优先级最低，最后做）

- 主人未回答细节问题，待确认：
  - 记录粒度：每首歌累计播放时长？还是每天/总时长？
  - 显示位置：歌曲信息弹窗？设置页面？统计页面？
  - 数据库：需要新建表还是在 songs 表加字段？

### 4. 迷你模式优化（需要视觉参考）

- 当前实现：300×80 控制条，封面缩略图 + 歌曲名 + 上一首/播放/下一首
- 主人想要类似 Apple Music 的迷你模式
- **主人说没有视觉模型无法描述，需要 Hermes 协助**
- 关键问题：
  - 窗口大小？
  - 要不要进度条？
  - 要不要歌词？
  - 布局是什么样？
- 相关文件：
  - `src/components/MiniPlayer.tsx`（263 行）
  - `src/styles/miniplayer.css`（184 行）
  - `src/stores/uiStore.ts`（isMiniMode 状态）
  - `src/App.tsx`（迷你模式切换逻辑，第133行）
  - `electron/ipc/window.ts`（窗口尺寸调整）

### 5. 动画特效全覆盖

- 主人未回答细节问题，待确认：
  - 覆盖范围：所有页面切换？组件出现/消失？
  - 目前有的动画：
    - 歌词界面进入/退出（滑入滑出，300ms）
    - 导航切换内容区淡入（180ms）
    - 歌词行微位移（420ms cubic-bezier 0.22, 1, 0.36, 1）
    - 迷你模式切换（200ms 淡入）
  - 缺动画的地方（猜测）：
    - 歌曲列表行出现/消失
    - 弹窗出现/消失
    - 专辑网格加载
    - 播放队列面板
    - 设置页面切换
  - 动画风格：统一用现有的 cubic-bezier 还是有其他偏好？

### 6. 性能占用优化

- 主人未回答是否有具体问题，待确认：
  - 有具体的性能问题吗？（CPU 高、内存增长、启动慢、滚动卡顿）
  - 还是提前优化？
  - 已知的性能相关设计：
    - currentTime 不放 Zustand（避免高频 re-render）
    - timeupdate 250ms 节流
    - RAF 循环直接操作进度条 DOM
    - 歌词索引只在变化时才 setState
    - AudioEngine 懒创建（用户交互后才创建）

### 7. 专辑排序

- 需求明确：专辑页面新增排序功能
- 排序维度：专辑首字母、歌手名字首字母
- 排序方向：升序、降序
- 当前状态：专辑页面没有排序，按歌曲数降序
- 相关文件：
  - `src/pages/Albums.tsx`（专辑页面，~190 行）
  - 排序控件参考：`src/components/SongList.tsx`（歌曲列表已有排序）

## 技术约束（必读）

- 不联网，纯本地
- 主进程禁止同步 I/O
- currentTime 不放 Zustand
- Worker 不能直接写 SQLite
- 自定义协议 `qinplayer://` 加载本地资源
- 不修改 `package.json` 依赖（除非主人确认）
- 测试用中文描述、动词开头

## 已有架构

详见 `SPEC.md` 的「架构总览」章节，包含：
- 运行时架构图
- 核心播放数据流
- 状态所有权表
- 进程与模块边界

## 开工前必读

1. `SPEC.md` — 项目规格书
2. `harness/CONSTRAINTS.md` — 代码约束
3. `harness/TEST_CONVENTIONS.md` — 测试规范
4. `docs/devlog/devlog-20260710-lyrics-three-fixes.md` — 最近 devlog

## 交接说明

- 主人需要 Hermes 协助确认第 4 项（迷你模式）的视觉需求
- 第 2、5、6 项主人还未回答细节问题，需要先确认再动手
- 第 7 项需求明确，可以先做
- 每项改动都需要写方案、主人确认后再执行
