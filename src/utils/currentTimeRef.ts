// =============================================================================
// QinPlayer — 当前播放时间共享 Ref
// =============================================================================
// 职责：存储当前播放时间，供多模块共享（playerStore 持久化、useAudioSync 更新）
// 设计：模块级 ref，不触发 React re-render，由 RAF 或定时器直接读写
// =============================================================================

/** 当前播放时间（秒），由 useAudioSync 的 timeupdate 回调更新 */
export const currentTimeRef = { current: 0 }
